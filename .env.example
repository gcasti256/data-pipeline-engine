# PostgreSQL connection (optional — only needed for postgres connector)
POSTGRES_DSN=postgresql://user:password@localhost:5432/dbname

# Monitoring dashboard
MONITOR_HOST=0.0.0.0
MONITOR_PORT=8080

# Pipeline metadata DB
PIPELINE_DB_PATH=pipeline_runs.db
